# template-materialUI-StartApp
Basic start app template using Material UI. This template only includes an App Bar, side Menu and basic components implementation such ass buttons, cards, layout and tables.
